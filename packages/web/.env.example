# Auth0 Regular Web Application (see gitreqd-cloud docs/identity.md).
# Generate AUTH0_SECRET: openssl rand -hex 32
AUTH0_DOMAIN=
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
AUTH0_SECRET=
# Local dev: http://localhost:3000 — on Vercel preview, omit to infer from the request host.
# APP_BASE_URL=
