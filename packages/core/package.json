{
  "name": "@gitreqd/core",
  "version": "0.4.0",
  "description": "JS facade over gitreqd-core WASM — parse, validate, format, and HTML",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "bash ../../scripts/build-wasm-core.sh && rm -rf dist && tsc && mkdir -p dist/wasm && cp wasm/gitreqd_wasm.js wasm/gitreqd_wasm_bg.wasm wasm/package.json dist/wasm/",
    "clean": "rm -rf dist wasm"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "requirements",
    "yaml"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "glob": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2"
  }
}
