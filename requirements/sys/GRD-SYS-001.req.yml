id: GRD-SYS-001
title: Parse requirement YAML
description: |
  The core engine shall parse requirement YAML files containing id, title, description,
  attributes, and links (including satisfies references).
attributes:
  rationale: |
    Parsing is the foundation for validation, reporting, and all consumers
    (CLI, VSCode, web, CI).
  status: active
